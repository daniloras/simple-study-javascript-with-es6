/**
 * arrow functions
 */

const exemple = [1, 2, 3, 4];

const arrowExemple = exemple.map((item) => item * 8);

console.log(arrowExemple);

// exemple

const test = () => ({ nome: "Danilo" });

console.log(test());